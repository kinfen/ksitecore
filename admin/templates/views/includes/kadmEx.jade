script.
	KAdm.model = !{model ? JSON.stringify(model.getOptions()) : ""};
	KAdm.navs = "!{navs}";
	KAdm.ItemId = "!{itemId}";
	KAdm.category = "!{category}";
	KAdm.page = !{page ? JSON.stringify(page) : null};
